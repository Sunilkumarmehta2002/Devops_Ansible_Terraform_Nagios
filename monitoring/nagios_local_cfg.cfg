define host{
  use                     linux-server
  host_name               provertos-web
  address                 __EC2_WEB_IP__
}

define host{
  use                     linux-server
  host_name               provertos-api
  address                 __EC2_API_IP__
}

define service{
  use                     generic-service
  host_name               provertos-web
  service_description     HTTP
  check_command           check_http!-I __EC2_WEB_IP__ -p 80
}

define service{
  use                     generic-service
  host_name               provertos-api
  service_description     API TCP 3001
  check_command           check_tcp!-H __EC2_API_IP__ -p 3001
}

define service{
  use                     generic-service
  host_name               provertos-api
  service_description     NRPE CPU
  check_command           check_nrpe!check_load
}
